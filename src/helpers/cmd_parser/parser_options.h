#pragma once

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

enum SyntheticGraphType
{
    RANDOM_UNIFORM_GRAPH = 0,
    RMAT_GRAPH = 1,
    SSCA2_GRAPH = 2,
    HPCG_GRAPH = 3,
    REAL_WORLD_GRAPH = 4
};

enum MatrixStorageFormat
{
    CSR = 0,
    COO = 1,
    CSR_SEG = 3,
    LAV = 4,
    VECT_GROUP_CSR = 5,
    SELL_C = 6
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
